import { ParticipantesClass } from './participantes.class';

describe('ParticipantesClass', () => {
  it('should create an instance', () => {
    expect(new ParticipantesClass()).toBeTruthy();
  });
});
